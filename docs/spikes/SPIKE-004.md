# SPIKE-004: Authenticated HPKE interoperability

Status: cryptographic core and real Chrome IndexedDB persistence validated

## Implementation

`src/crypto/auth-hpke.ts` wraps `@hpke/core` 1.9.0 with the suite proposed in ADR-002:

```text
Auth mode
DHKEM(P-256, HKDF-SHA256)
HKDF-SHA256
AES-128-GCM
info = "SyncNotifications-E2EE-v1"
```

`src/crypto/indexeddb-identity-store.ts` generates a non-extractable WebCrypto P-256 identity and persists its `CryptoKeyPair` through IndexedDB structured clone without exporting the private key.

`src/crypto/replay-guard.ts` retains the in-memory policy model. `src/crypto/indexeddb-replay-ledger.ts` implements the production persistence boundary with one atomic IndexedDB read/write transaction keyed by the 32-byte sender key ID and 16-byte message ID. It purges expired entries, serializes concurrent attempts, rejects duplicates after store reconstruction, and fails closed instead of evicting live entries at capacity.

`src/protocol/routing-header.ts` implements the fixed 160-byte big-endian Routing Header v1 codec. The exact encoded bytes are HPKE AAD; business message type and notification/action fields remain encrypted.

`src/protocol/encrypted-envelope.ts` implements the bounded binary frame. `src/crypto/envelope-receiver.ts` validates recipient identity and expiry, authenticates HPKE with the original header bytes, and returns plaintext only after the replay tuple is atomically accepted.

`src/protocol/encrypted-payload.ts` strictly validates canonical protobuf `action.invoke` and `action.result` payloads. `src/crypto/action-envelope-sender.ts` durably registers pending correlation and exact canonical invoke bytes before constructing a per-recipient encrypted frame. `src/crypto/indexeddb-pending-action-store.ts` retains the idempotency-key/expected-Android/key-ID/canonical-operation-digest and delivery retry state across Worker restarts, preventing accidental reuse of one key for different action semantics. `src/crypto/indexeddb-outbound-sequence-store.ts` atomically allocates positive per-recipient routing sequences. `src/crypto/action-invoke-outbox.ts` sends only to a still-approved Android pin over a `SNO1`-authenticated socket, recreates each retry with a fresh envelope message ID/sequence but the same canonical operation and business idempotency key, and preserves unsent work across Worker reconstruction. `src/crypto/action-result-receiver.ts` authenticates and atomically reconciles returned results, which immediately excludes completed operations from outbound delivery.

`src/protocol/trusted-device-pairing.ts` implements the server-independent canonical offer/approval records and `sntrust1:` QR representation used by the approval workflow. It validates the complete P-256 point on-curve, non-zero identifiers/nonces, safe bounded timestamps, exact offer hash, distinct device/key pairs, canonical base64url, and the transcript-derived 60-bit safety code. Decoding or scanning does not write `IndexedDbTrustedPeerStore`; explicit confirmation remains a separate required step.

`src/crypto/indexeddb-trust-pairing-session-store.ts` persists exactly one active offerer or approver transcript and refuses silent replacement. `trust-pairing-coordinator.ts` binds every session to the current local workspace/device/public key, restores it after Worker reconstruction, rejects expired/cross-workspace/self-pairing input, requires the caller to explicitly confirm the exact active safety code, and only then writes the immutable peer pin. It removes the exact session after pinning; if removal is interrupted, retry converges through `already-pinned` without key replacement.

`src/transport` now matches the Go `SNA1`/`SNO1` vector, performs bounded strict code-gated registration without credentials or referrers, stores the raw WebSocket credential only in extension-origin IndexedDB, refuses silent replacement, and sends `SNA1` as the first binary message. It consumes and validates fixed `SNO1` within five seconds before emitting the content-free `authenticated` diagnostic, so socket-open/local-enqueue is never presented as server acceptance. HTTPS is mandatory outside loopback, redirect/endpoint changes fail closed before credential disclosure, and diagnostics expose only fixed content-free state events.

The Options page now requests optional access only to the selected canonical server origin, creates/restores the non-extractable HPKE identity, consumes an administrator-issued code, and starts the Worker transport runtime after durable registration. Worker startup restores the credential only when its full SHA-256 identity key ID matches the existing HPKE public key; it never creates a replacement identity for partial state. The runtime reports `online` only after `SNO1` and clears its loaded raw token copy after constructing the first frame.

Inbound frames are serialized through `ActionResultDispatcher`. Before HPKE opening, it strictly decodes the bounded `SNE1` frame, checks workspace and recipient against the active credential, and resolves the exact sender device/key tuple only through `IndexedDbTrustedPeerStore`. That store accepts only valid P-256 points supplied by a separate approval workflow, is idempotent for the same pin, and refuses key replacement until explicit removal. An approved frame then passes through Auth HPKE, persistent replay consumption, canonical `action.result` validation, and pending-action reconciliation. Unknown pins or any asynchronous dispatch failure close the socket fail-closed. The durable approval core is connected to Options using the existing transport credential and non-extractable HPKE identity, with their SHA-256 key-ID relationship revalidated before every operation. Options supports offer creation, controlled `sntrust1:` payload copy/import, Worker-independent session restoration, full safety-code display, a mandatory explicit comparison checkbox, local approval, rejection/cancellation, and fail-closed recovery. Loaded transport-token copies are zeroed. Camera QR rendering/scanning is not implemented yet; textual payload exchange is only the controlled integration path for the upcoming synthetic relay test, and no server-provided entry is trusted automatically.

Network/socket termination now schedules one jittered exponential reconnect sequence bounded from 1 to 60 seconds. Duplicate `error`/`close` events for a connection generation cannot create duplicate retries, successful `SNO1` authentication resets the sequence, explicit lifecycle requests cancel stale timers, and persistent identity-binding failures do not retry. The production Worker backs the scheduler with named `chrome.alarms` one-shots so connection and 1/2/4/8-second action-delivery retries can wake an MV3 Worker after suspension; generation checks make obsolete in-memory callbacks harmless. `TransportRuntime.sendEnvelope` refuses every pre-`SNO1`, stale-generation, closed, or absent socket. Explicit test resends that wake a suspended Worker rebuild transport and wait up to five seconds for `SNO1` before retrying the exact durable operation. Local encrypted-delivery corruption invokes `failClosed` and does not masquerade as a network retry.

Authenticated result reconciliation computes SHA-256 over the exact canonical `EncryptedPayload(action_result)` plaintext and commits terminal reconciliation plus the canonical `action_result_ack` intent in the same IndexedDB transaction. `ActionResultAckOutbox` retains recipient device/key binding, uses fresh durable sequence/message tuples, rechecks the immutable pin before encryption, retries with a bounded schedule across Worker wake, and never treats local send acceptance as Android deletion. Duplicate identical results reactivate the same ACK intent; ACK never creates ACK-of-ACK. Options exposes only redacted delivery counts, persists the selected synthetic operation across page/Worker reload, and provides an explicit authenticated-transport reconnect control. Version `0.1.11` presents every result as a terminal status rather than equating completion with success. In particular, `OUTCOME_UNKNOWN` receives a persistent warning that the side effect may or may not have occurred, Android will not execute the operation again, and ACK proves only durable reconciliation of that uncertainty. Its recovery control requests the exact result under the existing business idempotency key and explicitly states that no re-execution occurs; it cannot create a replacement operation.

## Evidence

- Chrome opens the Android-produced fixture.
- Android opens the deterministic Chrome-produced fixture.
- Deterministic key, encapsulation, and ciphertext bytes match the canonical vector.
- Sender substitution, AAD modification, and ciphertext modification fail.
- Non-extractable identity persistence/restore and authenticated HPKE use pass with IndexedDB unit coverage.
- Persistent replay reconstruction, concurrent duplicate attempts, expiry, capacity, and identifier validation pass with IndexedDB unit coverage.
- TypeScript matches the Go/Kotlin Routing Header v1 vector, rejects malformed fields, and proves that changing a routing byte breaks HPKE authentication.
- TypeScript matches the Encrypted Envelope v1 vector and rejects truncation, trailing bytes, bad magic, invalid points, and invalid ciphertext lengths.
- Receiver tests prove tampered HPKE ciphertext does not consume replay state, a valid frame is accepted once, and its repeat is rejected.
- TypeScript matches the canonical protobuf action payload, rejects unknown/duplicate/non-canonical and invalid semantic fields, round-trips `action.result`, and round-trips a generated authenticated action envelope without exposing notification ID or reply bytes in the frame.
- TypeScript matches Go/Kotlin's fixed Trusted Device Pairing v1 offer, approval, QR, and `4AFH-Q91K-PGVG` safety-code vector. Exact transcript mutation, wrong offer hash, invalid P-256 point, expired record, padded QR, and whitespace fail closed; no test path creates an approved-peer pin by scanning alone.
- Pairing coordinator tests reconstruct both offerer and approver stores, verify identical safety codes, prove a wrong confirmation leaves both the pin absent and the durable session present, then confirm each side independently and verify reciprocal immutable pins plus exact session removal. Cross-workspace and expired offers create no approver session. Approved-peer lookup now also recomputes the persisted public-key SHA-256 and validates the curve point before returning a pin, so corrupted key binding fails closed.
- Pending-operation tests cover persistence across reconstruction, concurrent registration, expected-sender binding, capacity exhaustion, identical result recovery, conflicting terminal results, exact canonical invoke persistence, bounded retry/dormancy, reactivation, and terminal-result delivery suppression.
- Action-invoke outbox tests prove persistence occurs before send, pre-`SNO1` transport send refusal, fresh persistent sequences for retries, exact idempotency-key recovery after reconstruction, runtime Auth HPKE opening by the pinned recipient identity, and pin removal preventing subsequent encryption.
- The action-result receive path performs HPKE/replay validation before persistent reconciliation; a new envelope carrying the same authenticated result is idempotent.
- Registration, credential reconstruction/replacement refusal, secure-origin validation, response bounds, `SNA1` codec, first-message ordering, and endpoint-change credential non-disclosure are covered.
- Type checking, 67 Vitest tests across 23 files, protocol verification, and the production build pass locally. GitHub Actions run `31555248488` independently passes all four gates for clarified-flow commit `ed687f1`.
- On 2026-08-12, the user completed the blocking synthetic approval workflow against Android in one freshly created workspace. Chrome registered and reached `Online`, created the offer, accepted Android's distinct approval response, and displayed the same complete safety code as Android. Android was approved first without changing Chrome's independent pending state. After closing and reopening Options, Chrome restored the same Step 3 transcript and safety code, then explicit local confirmation produced `Peer approved locally. The other device must confirm independently.` This is user-observed UI evidence, separate from automated IndexedDB tests; no real notification or reply content was used.
- On 2026-08-13, the user completed the blocking synthetic relay action/result loop in that approved workspace. Options accepted only the app-owned Android target matching the sole approved pin, durably queued the invoke, and ultimately displayed `Authenticated Android result reconciled. Status code: 1.` Status `1` is the canonical `ACTION_RESULT_STATUS_SUCCEEDED` value. Android independently displayed `Debug receiver: Regular PendingIntent action received`, proving that Chrome reconciliation was not inferred from local WebSocket acceptance. This manual evidence is distinct from automated pending/replay tests; no real notification, reply, or third-party operation was used.
- Later on 2026-08-13, the user completed the blocking exact-duplicate relay check with extension version `0.1.1`. A fresh operation reached terminal authenticated status `1`; before explicit resend, Chrome reported two locally accepted invoke deliveries and Android's persisted redacted side-effect count increased exactly once from `2` to `3`. Selecting `Resend exact completed action` then increased Chrome's durable accepted invoke-delivery count from `2` to `3` and authenticated result-delivery count from `10` to `11`, while Android's side-effect count remained `3`. The explicit resend re-used the exact canonical invoke and business idempotency key under a fresh envelope tuple, and Android returned its cached canonical result without a second side effect. Result-delivery count is intentionally not treated as operation count while end-to-end ACK is absent.
- The same session then completed the blocking MV3 Worker restart relay check. `chrome://serviceworker-internals` showed the extension Worker already stopped; the user selected `Start`, authenticated transport recovered, and the still-open Options page resent the same durable completed operation. Accepted invoke-delivery count increased from `3` to `4`, authenticated result-delivery count from `11` to `20`, Android's persisted side-effect count remained `3`, and terminal result status remained `1`. This is real-browser evidence that Worker reconstruction restores the credential-bound identity, approved pin, durable exact operation, and outbound sequence sufficiently to traverse the relay without re-executing the Android side effect.
- The blocking Android process restart relay check then force-stopped the app process; its PID changed from `12342` to `17515` when Android recreated the authorized notification-listener process. Without creating a new notification, target, or operation, the user waited for transport recovery and resent the same durable completed operation. Accepted invoke-delivery count increased from `4` to `5`, authenticated result-delivery count from `20` to `25`, terminal status remained `1`, and Android's persisted side-effect count remained `3`. Chrome thus reconciled the durable cached success after Android process death without a second side effect.
- The blocking interrupted-network durable-queue check then stopped the loopback relay with both clients retaining durable state. An accidental pre-outage operation had already raised Android's persisted side-effect baseline from `3` to `4`; the actual test operation was separately queued while both clients showed offline. Options reported `Still pending` with zero locally accepted attempts, and Android remained at `4`. The same `manual-same-workspace.db` relay then restarted; without Retry, Connect, another Queue, or extension reload, both connections returned and the durable pending operation automatically reconciled as status `1`, with two locally accepted invoke deliveries and five authenticated result deliveries. Android's count increased exactly once from `4` to `5`. This proves offline queue persistence and authenticated automatic recovery without replacing the business operation or duplicating its side effect.
- The blocking Chrome-side pin-removal relay check used extension `0.1.2` and a fresh successful synthetic operation that raised Android's persisted side-effect count from `5` to `6`. Once delivery counters settled at one accepted invoke and five authenticated results, the user explicitly removed the sole approved Android pin through the confirmation-gated Options control. Exact resend returned `Exact resend rejected before encryption: the Android recipient is not approved.` Refresh left the completed status `1`, invoke count `1`, and result count `5` unchanged, while Android remained at `6`. This proves immediate fail-closed removal before HPKE encryption or socket delivery while transport admission remains separate.
- Reciprocal trust was then restored only through a fresh Offer/Approval transcript. The user confirmed identical complete safety codes on both devices and approved independently; Android's existing identical Chrome pin converged idempotently while Chrome re-pinned the same Android identity. A fresh post-recovery operation completed with authenticated status `1`, one accepted invoke delivery, and one authenticated result delivery, and Android's persisted side-effect count increased exactly once from `6` to `7`. The baseline, duplicate, MV3 Worker restart, Android process restart, interrupted-network durable queue, pin removal, and re-approval relay matrix is therefore manually validated.
- On 2026-08-17, extension `0.1.7` completed the blocking end-to-end result-ACK relay validation with the fixed-signature Android build. A fresh operation reconciled status `1` with one accepted invoke and one authenticated result; Chrome created the durable ACK intent and locally accepted its bounded deliveries. Android independently returned to its exact historical live-outbox baseline while its synthetic side-effect count increased once. After a real extension reload, Options restored the selected durable operation. The first explicit resend attempts exposed that a Worker-wake message could arrive before `SNO1`; `0.1.7` now waits for authenticated transport and preserves its immediate diagnostic for five seconds. The corrected exact resend was accepted, raising invoke/result deliveries from `1/1` to `2/2`, while Android's side-effect count and outbox baseline remained unchanged. This proves exact operation reuse, duplicate result reactivation of the ACK intent, Android duplicate-ACK idempotency, and fail-closed pre-auth send behavior in the real browser/relay/device path.
- The next automated recovery slice rejects an ACK frame while the socket is unavailable, verifies no accepted-attempt counter is advanced, reconstructs the pending-action, trusted-peer, outbound-sequence, and ACK-outbox objects as an MV3 Worker restart would, and reads the byte-identical canonical ACK intent from IndexedDB. Once authenticated sending becomes available, the reconstructed outbox emits a fresh envelope at the next durable sequence and continues its bounded schedule.
- The 2026-08-17 blocking ACK interruption matrix used extension `0.1.8`'s memory-only, ten-minute, exact-operation test hold, which refuses any canonical invoke not bound to the selected business key and the app-owned `dev.notificationmirroring.android` notification. A duplicate result reactivated the ACK intent while ACK accepted deliveries stayed zero. Android independently changed from `reservations/completed/acknowledged/accepted-sends = 9/9/2/45` to `10/10/1/50` with side-effect count fixed at `9`. The relay was stopped, Android PID changed from `31803` to `2868`, and extension reload destroyed the hold while IndexedDB restored the completed operation, exact ACK intent, and zero accepted deliveries. With the relay still offline no ACK was misreported. Explicit authenticated reconnect then delivered five bounded duplicate ACKs and Android returned exactly to `9/9/2/45`, proving safe final convergence across Android process restart, Chrome Worker restart, and network interruption.
- That run also found that alarm-only recovery could strand a browser socket whose cached state still appeared authenticated after MV3 suspension. `0.1.9`'s pre-auth watchdog was insufficient and failed a second no-input relay recovery test. Server/extension `SNH1`/`SNH2` authenticated heartbeat support in `0.1.10` now sends every 20 seconds, requires a response within 10 seconds, consumes both frames outside encrypted-envelope dispatch, and contains no identifiers or business metadata. In the final blocking retest, the relay remained unavailable for more than 70 seconds; after the same registry returned, server-side connections automatically increased from one to two in approximately 45 seconds without Reload, Reconnect, or other browser input, and the user independently observed `0.1.10 auto-recovered Online`. Android stayed exactly at `9/9/2/45`, `ONLINE`, and side-effect count `9`.
- `0.1.11` adds a pure, exhaustively mapped action-result presentation boundary. Automated UX assertions distinguish local `PendingIntent.send()` acceptance from third-party business success, render `OUTCOME_UNKNOWN` as terminal and non-repeatable, state that ACK cannot turn uncertainty into success, label exact-result recovery as no-re-execution, and fail closed for absent or future unsupported status codes. Current automation is 26 files / 76 tests plus typecheck, production build, and protocol verification. A synthetic real-relay crash is not required for this UI slice because Android crash-to-uncertainty and Chrome authenticated reconciliation are already covered independently; no debug bypass was added to manufacture an unauthenticated result.
- The E2EE Identity Key Transition v1 protocol is vendored with spec hash `665e676d3ac0620cdb10d48a0e0c5ecc1d0ffb2cb1997caf724e9e94a33fb323` and vector hash `f87f605480d320b622d3810a250f445b40ba0bc6aec27a2a6f5630f87d29c3d0`. Generated TypeScript and a dedicated async canonical codec support schema-v2 transition/ACK/commit without admitting identity bodies through the existing schema-v1 action codec. Validation covers unknown/non-canonical bytes, fixed non-zero IDs and digests, P-256 point membership, exact WebCrypto SHA-256 public-key binding, distinct old/new keys, and schema/body mismatch. All three encodings match Go and Android. `IndexedDbIdentityStore` now migrates the legacy single `CryptoKeyPair` record on first pending preparation into one atomic current/pending record. Both private keys remain non-extractable; concurrent preparation converges on one exact pending identity, Worker-style store reconstruction restores it for HPKE decryption, current remains unchanged, and same-key corruption fails closed. Cross-database promotion is intentionally not exposed until the transport identity binding has a recoverable commit protocol. A dedicated proposed-local-identity receive boundary now authenticates and decrypts only a canonical `identity_key_transition_ack` addressed to the pending key, then verifies the exact peer device, transition ID, old/new key IDs, and transition digest with a full-length byte comparison before replay consumption. Authenticated schema-v1 business plaintext, another lifecycle body, a wrong peer device, or any wrong transition binding fails closed without reaching a business dispatcher. The trusted-peer IndexedDB schema now migrates from v1 to v2 and atomically retains one exact successor per approved peer: canonical transition bytes/digest, old/new key binding, proposed public key, deterministic canonical ACK intent/digest, seven-day expiry, and `pending-commit`/`blocked` phase. Exact duplicate delivery returns the same ACK bytes after Worker/store reconstruction; a different successor fails closed. Expiry durably blocks without replacing either pin, while explicit peer removal deletes both records. The authenticated transition receiver persists the successor and ACK intent before replay consumption, so replay-capacity failure or a Worker crash cannot lose an authenticated transition; retry under a fresh envelope tuple remains idempotent. Trusted-peer schema v3 adds durable ACK retry scheduling with v2 migration. `IdentityTransitionAckOutbox` requires the current non-extractable sender identity to match the transport identity binding, encrypts the exact stored ACK to the proposed new key, and allocates a fresh message ID and durable recipient-key sequence for each delivery. `send == false` leaves the intent immediately due; local socket acceptance only advances bounded exponential retry metadata and never deletes the ACK. Five accepted attempts make it dormant until an exact transition retry reactivates the same ACK bytes. Worker/store reconstruction preserves exact plaintext and sequence continuity, while blocked transitions are never due. `IndexedDbLocalIdentityTransitionStore` now persists one rotating-device session with its immutable peer snapshot and seven-day deadline. The pending-identity receiver authenticates an ACK against that snapshot, then atomically saves the exact canonical ACK, its SHA-256, and the exact peer-specific canonical commit before consuming replay. Replay-capacity failure therefore leaves recoverable commit work; a fresh-envelope retry returns the byte-identical commit after Worker/store reconstruction. Wrong peer/key/transition/digest and non-canonical ACKs fail closed, while expiry durably blocks without generating a commit or modifying either identity. Peer-side commit-only pending-successor routing, commit delivery, pin promotion/tombstone, cross-database promotion, and recovery UI remain pending. Current automation is 32 files / 102 tests plus typecheck, production build, and protocol verification.
- `0.1.12` vendors Transport Credential Rotation v1 at canonical hash `6f8fb759ce11ca2b7f8470b830ae99feaf199ec2272318224deb2ae0cd190294`. One IndexedDB record atomically retains current, at most one pending secret, and `prepared`/`attempted` phase. Options generates pending and commits attempted before the exact no-redirect HTTPS request; neither HTTP send nor HTTP 200 promotes. Ambiguous loss and Worker reconstruction reuse exact pending. Runtime probes attempted pending first, falls back to current after pre-`SNO1` denial, and promotes only after pending receives exact `SNO1`; promotion atomically removes old current and rotation metadata. Device tuple and HPKE identity binding are unchanged, loaded secret copies are cleared, and malformed/changed endpoint or response fails closed. Automation covers reconstruction, replacement refusal, request-before-send ordering, response loss, exact retry bytes, fallback, pending `SNO1` promotion, and current retention. Current automation is 27 files / 85 tests plus typecheck, production build, and protocol verification.
- On 2026-08-18, the blocking isolated-browser rotation matrix passed against the real LAN TLS relay. Chrome completed a baseline rotation and remained `online` after exact pending `SNO1`, MV3 Worker termination, and full browser restart. With the relay unavailable, Options retained an `attempted` pending record; Worker reconstruction reused the exact pending secret, pending denial fell back to unchanged current and returned `online`, and retrying the same authorization promoted only after `SNO1`. A TLS test proxy then committed the server transaction, dropped the HTTP response, and blocked new WSS for 30 seconds. The Worker was terminated after commit; reconstruction authenticated and promoted the durable pending without relying on HTTP success. Finally, a pre-rotation IndexedDB record was restored under a new Worker and remained `offline`, while restoring the latest record returned `online`, proving permanent rejection of the old credential. Server credential versions advanced from `1` through `5`; final storage had no pending/phase. Stable digests proved the non-extractable HPKE identity and approved Android pin were unchanged, and exact non-secret tuple comparison proved workspace, device ID, and HPKE identity binding were unchanged. Only synthetic data and an isolated Cent Browser profile were used; real notification traffic remains forbidden by separate gates.
- The blocking non-loopback matrix used an isolated Cent Browser profile, a fresh HTTPS/WSS-bound credential and E2EE identity, a dedicated workspace, a certificate valid for the exact LAN endpoint, and zero `adb reverse` entries. After an initial browser policy screen was resolved, the user independently observed `0.1.11 Online`; reciprocal E2EE safety codes matched and both peers approved independently. One app-owned synthetic action reached terminal `Succeeded` with the new caveat that local `PendingIntent.send()` acceptance is not third-party business completion; Android observed exactly one side effect and exact result-ACK convergence. The relay then remained absent for more than 112 seconds. Restoring the same database/certificate/origin returned both server-side WSS connections in approximately 33 seconds without Reconnect, Reload, Options input, or browser restart, and the user independently observed `0.1.11 non-loopback auto-recovered Online`. Android's outbox and side-effect aggregates were byte-for-byte semantically unchanged. No real notification, reply, or third-party action was used. The isolated browser was then stopped and its entire profile deleted; clipboard payloads, dedicated database, codes, credentials, TLS/CA keys, and logs were removed, and both Windows and Android temporary CA trust entries were explicitly removed.
- Popup exposes a browser-runtime persistence test; repeated runs retain the same fingerprint.

## Browser runtime evidence

Manual validation completed on 2026-08-06 using the unpacked production build
on Windows Chrome (exact browser version was not captured):

| Scenario | Public-key fingerprint | Private extractable | HPKE round trip | UTC time |
| --- | --- | --- | --- | --- |
| Initial run | `79692691f6d994b5c9a5e838f15cd1fd7e9fced991cfcbd33e0497513ba5b76c` | `false` | `true` | `2026-08-06T06:41:39.515Z` |
| After MV3 Worker termination | same | `false` | `true` | `2026-08-06T06:45:31.243Z` |
| After full Chrome restart | same | `false` | `true` | `2026-08-06T06:46:07.868Z` |

The unchanged SHA-256 public-key fingerprint proves that IndexedDB restored the
same non-extractable WebCrypto private identity rather than silently generating
a replacement.

Persistent replay-ledger validation completed on 2026-08-06:

| Scenario | Replay decision | Public-key fingerprint | UTC time |
| --- | --- | --- | --- |
| First authenticated tuple | `accepted` | `79692691f6d994b5c9a5e838f15cd1fd7e9fced991cfcbd33e0497513ba5b76c` | `2026-08-06T07:52:25.681Z` |
| Immediate repeat | `duplicate` | same | `2026-08-06T07:52:37.784Z` |
| After full Chrome restart | `duplicate` | same | `2026-08-06T07:53:34.190Z` |

Chrome did not expose a separately terminable Worker task during this run, so
an explicit Worker-only termination was not performed. A full browser exit
necessarily terminated the MV3 Worker and the subsequent `duplicate` decision
proves that the replay record was restored from IndexedDB rather than Worker
memory. All three runs also reported a non-extractable private key and a
successful HPKE round trip.

Authenticated transport runtime validation completed on 2026-08-07 with the
unpacked production build, the loopback test server, a dedicated SQLite test
registry, and synthetic registration data only. The user explicitly concluded
the test passed: registration reached `Online`, the Popup remained `Online`, and
a full Chrome restart increased the persisted `Worker starts observed` counter
to `16` while the authenticated connection recovered and stayed `Online`.
This is manual observation evidence, separate from the 48 automated tests; no
real notification content was transmitted.

Bounded reconnect validation completed on 2026-08-11 with the latest unpacked
production build, the same dedicated loopback registry, and no business frames.
The user observed the restored connection reach `Online`, then observed
`Offline` after the server stopped. The server remained unavailable for an
additional measured 35 seconds to cover MV3 Worker suspension before becoming
ready again. Without clicking connect/retry, reloading the extension, or opening
Options, the user then observed automatic recovery to `Online` within the
60-second maximum backoff window. This manual UI conclusion is recorded
separately from the 57 automated tests covering generation deduplication,
bounded delay, alarm wake handling, approved sender dispatch, replay, and
pending-result reconciliation.

Vendored vectors:

```text
protocol/test-vectors/hpke-auth-p256-aes128gcm.json
protocol/test-vectors/routing-header-v1.json
protocol/test-vectors/encrypted-payload-v1.json
protocol/test-vectors/encrypted-envelope-v1.json
protocol/test-vectors/trusted-device-pairing-v1.json
```

The authoritative copy and ADR-002 live in the server repository.

## Safety boundary

`SerializedHpkeKeyPair`, fixed IKM, and deterministic `ekm` are spike/test facilities. Production code now uses a non-extractable WebCrypto identity key plus persistent replay, trusted-peer-pin, pending-operation/delivery, and sequence stores in separate IndexedDB databases. The Popup runtime test records a fixed authenticated tuple once and must report `duplicate` after Worker/browser restart. A transport timeout leaves an operation pending and retries the exact operation under the same idempotency key; only Android's authenticated `OUTCOME_UNKNOWN` result marks the side effect as uncertain. Local WebSocket acceptance proves neither Android execution nor Chrome reconciliation. The approved synthetic relay action/result loop, baseline/exact-duplicate end-to-end result ACK, and real-relay ACK interruption/Android-process/Chrome-Worker recovery matrix are manually validated. Durable ACK store reconstruction is also automated, and `SNH1`/`SNH2` closes the observed MV3 stale-socket liveness gap. Real notification payloads remain blocked until the separate general relay cursor, pairing rotation/revocation, multi-device convergence, non-loopback recovery, and security review are complete.
